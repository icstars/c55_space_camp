var array_of_names = ["Andy", "Chase", "Denise", "Eli", "Francis", "Jort"];

function multiplyBy2(x) {
    return x * 2;
}

var i = 1;

var j = multiplyBy2(i);

var k = multiplyBy2(j);

console.log(array_of_names[k]);